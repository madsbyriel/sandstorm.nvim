*sandstorm.nvim.txt*
                             For Neovim >= 0.8.0    Last change: 2026 April 15

==============================================================================
Table of Contents                             *sandstorm.nvim-table-of-contents*

1. Prerequisites                                  |sandstorm.nvim-prerequisites|
2. Installing                                        |sandstorm.nvim-installing|
  - Using vim.pack (Neovim 0.12+)|sandstorm.nvim-installing-using-vim.pack-(neovim-0.12+)|
  - Using packer                        |sandstorm.nvim-installing-using-packer|
  - Using lazy.nvim                  |sandstorm.nvim-installing-using-lazy.nvim|
  - Using vim-plug                    |sandstorm.nvim-installing-using-vim-plug|
3. Basic Usage                                      |sandstorm.nvim-basic-usage|
4. Configuration                                  |sandstorm.nvim-configuration|
  - Overriding                         |sandstorm.nvim-configuration-overriding|
>
      <h1> <img src="https://i.postimg.cc/WpQzgxVh/plugin-Icon.png" width="80px"><br/>sandstorm.nvim</h1>
     </div>
<


A port of sandstorm community <https://github.com/sandstorm-community/sandstorm>
theme to lua with treesitter
<https://github.com/nvim-treesitter/nvim-treesitter> and |semantic highlights|
support!


==============================================================================
1. Prerequisites                                  *sandstorm.nvim-prerequisites*

Neovim 0.8.0+


==============================================================================
2. Installing                                        *sandstorm.nvim-installing*


USING VIM.PACK (NEOVIM 0.12+)*sandstorm.nvim-installing-using-vim.pack-(neovim-0.12+)*

>lua
    vim.pack.add({
      "https://github.com/ellisonleao/sandstorm.nvim"
    })
    
    require("sandstorm").setup()
    vim.cmd.colorscheme("sandstorm")
<


USING PACKER                            *sandstorm.nvim-installing-using-packer*

>lua
    use { "ellisonleao/sandstorm.nvim" }
<


USING LAZY.NVIM                      *sandstorm.nvim-installing-using-lazy.nvim*

>lua
    { "ellisonleao/sandstorm.nvim", priority = 1000 , config = true, opts = ...}
<


USING VIM-PLUG                        *sandstorm.nvim-installing-using-vim-plug*

>vim
    Plug 'ellisonleao/sandstorm.nvim'
<


==============================================================================
3. Basic Usage                                      *sandstorm.nvim-basic-usage*

Inside `init.vim`

>vim
    set background=dark " or light if you want light mode
    colorscheme sandstorm
<

Inside `init.lua`

>lua
    vim.o.background = "dark" -- or "light" for light mode
    vim.cmd([[colorscheme sandstorm]])
<


==============================================================================
4. Configuration                                  *sandstorm.nvim-configuration*

Additional settings for sandstorm are:

>lua
    -- Default options:
    require("sandstorm").setup({
      terminal_colors = true, -- add neovim terminal colors
      undercurl = true,
      underline = true,
      bold = true,
      italic = {
        strings = true,
        emphasis = true,
        comments = true,
        operators = false,
        folds = true,
      },
      strikethrough = true,
      invert_selection = false,
      invert_signs = false,
      invert_tabline = false,
      inverse = true, -- invert background for search, diffs, statuslines and errors
      contrast = "", -- can be "hard", "soft" or empty string
      palette_overrides = {},
      overrides = {},
      dim_inactive = false,
      transparent_mode = false,
    })
    vim.cmd("colorscheme sandstorm")
<

**VERY IMPORTANT**: Make sure to call setup() **BEFORE** calling the
colorscheme command, to use your custom configs


OVERRIDING                             *sandstorm.nvim-configuration-overriding*


PALETTE ~

You can specify your own palette colors. For example:

>lua
    require("sandstorm").setup({
        palette_overrides = {
            bright_green = "#990000",
        }
    })
    vim.cmd("colorscheme sandstorm")
<


HIGHLIGHT GROUPS ~

If you don’t enjoy the current color for a specific highlight group, now you
can just override it in the setup. For example:

>lua
    require("sandstorm").setup({
        overrides = {
            SignColumn = {bg = "#ff9900"}
        }
    })
    vim.cmd("colorscheme sandstorm")
<

It also works with treesitter groups and lsp semantic highlight tokens

>lua
    require("sandstorm").setup({
        overrides = {
            ["@lsp.type.method"] = { bg = "#ff9900" },
            ["@comment.lua"] = { bg = "#000000" },
        }
    })
    vim.cmd("colorscheme sandstorm")
<

Please note that the override values must follow the attributes from the
highlight group map, such as:

- **fg** - foreground color
- **bg** - background color
- **bold** - true or false for bold font
- **italic** - true or false for italic font

Other values can be seen in |`synIDattr`|

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
